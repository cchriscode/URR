apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: queue-service
  namespace: urr-spring
  labels:
    app: queue-service
spec:
  replicas: 3
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: queue-service
  template:
    metadata:
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3007"
        prometheus.io/path: "/actuator/prometheus"
      labels:
        app: queue-service
        tier: backend
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: queue-service
          image: YOUR_ECR_URI/queue-service:latest
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
          ports:
            - containerPort: 3007
          envFrom:
            - configMapRef:
                name: spring-prod-config
            - secretRef:
                name: spring-prod-secret
          env:
            - name: PORT
              value: "3007"
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
            - name: TICKET_SERVICE_URL
              value: "http://ticket-service:3002"
            - name: ZIPKIN_ENDPOINT
              value: "http://zipkin-spring:9411/api/v2/spans"
            - name: SQS_ENABLED
              valueFrom:
                configMapKeyRef:
                  name: spring-prod-config
                  key: SQS_ENABLED
            - name: SQS_QUEUE_URL
              valueFrom:
                configMapKeyRef:
                  name: spring-prod-config
                  key: SQS_QUEUE_URL
            - name: VWR_DYNAMODB_ENABLED
              valueFrom:
                configMapKeyRef:
                  name: spring-prod-config
                  key: VWR_DYNAMODB_ENABLED
                  optional: true
            - name: VWR_DYNAMODB_COUNTERS_TABLE
              valueFrom:
                configMapKeyRef:
                  name: spring-prod-config
                  key: VWR_DYNAMODB_COUNTERS_TABLE
                  optional: true
            - name: TRACING_SAMPLING_PROBABILITY
              value: "0.1"
          startupProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 3007
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 3007
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 3007
            periodSeconds: 20
          resources:
            requests:
              cpu: "200m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
  strategy:
    blueGreen:
      activeService: queue-service
      previewService: queue-service-preview
      autoPromotionEnabled: false
      prePromotionAnalysis:
        templates:
          - templateName: health-check
        args:
          - name: service-name
            value: queue-service-preview
          - name: port
            value: "3007"
      scaleDownDelaySeconds: 30
