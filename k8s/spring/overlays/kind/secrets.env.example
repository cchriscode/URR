# === Database credentials ===
# Shared PostgreSQL superuser
POSTGRES_USER=urr_user
POSTGRES_PASSWORD=<change-me>

# Auth service database
AUTH_DB_USERNAME=urr_user
AUTH_DB_PASSWORD=<change-me>

# Ticket service database
TICKET_DB_USERNAME=urr_user
TICKET_DB_PASSWORD=<change-me>

# Payment service database
PAYMENT_DB_USERNAME=urr_user
PAYMENT_DB_PASSWORD=<change-me>

# Stats service database
STATS_DB_USERNAME=urr_user
STATS_DB_PASSWORD=<change-me>

# Community service database
COMMUNITY_DB_USERNAME=urr_user
COMMUNITY_DB_PASSWORD=<change-me>

# === Authentication ===
# JWT signing secret (must be Base64-encoded, minimum 32 bytes)
JWT_SECRET=<base64-encoded-secret-min-32-bytes>

# Internal service-to-service API token
INTERNAL_API_TOKEN=<change-me>

# === Queue / VWR ===
# HMAC secret for queue entry tokens (minimum 32 characters)
QUEUE_ENTRY_TOKEN_SECRET=<min-32-chars-secret>

# === Payment ===
# Toss Payments client key
TOSS_CLIENT_KEY=<toss-client-key>
