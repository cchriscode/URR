name: Ticket Service CI/CD

on:
  push:
    branches: [main]
    paths:
      - 'services-spring/ticket-service/**'
      - '.github/workflows/ticket-service-ci-cd.yml'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - prod

jobs:
  prod-approval:
    name: Production Approval
    if: github.event.inputs.environment == 'prod'
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Approved
        run: echo "Production deployment approved for ticket-service"

  ci-cd:
    needs: [prod-approval]
    if: always() && (needs.prod-approval.result == 'success' || needs.prod-approval.result == 'skipped')
    uses: ./.github/workflows/reusable-spring-ci-cd.yml
    with:
      service-name: ticket-service
      ecr-repository: urr/ticket
      service-path: services-spring/ticket-service
      environment: ${{ github.event.inputs.environment || 'staging' }}
    secrets: inherit
