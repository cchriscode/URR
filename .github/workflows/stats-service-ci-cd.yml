name: Stats Service CI/CD

on:
  push:
    branches: [main]
    paths:
      - 'services-spring/stats-service/**'
      - '.github/workflows/stats-service-ci-cd.yml'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - prod

jobs:
  ci-cd:
    uses: ./.github/workflows/reusable-spring-ci-cd.yml
    with:
      service-name: stats-service
      ecr-repository: urr/stats
      service-path: services-spring/stats-service
      environment: ${{ github.event.inputs.environment || 'staging' }}
    secrets: inherit
