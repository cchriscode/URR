name: Payment Service CI/CD

on:
  push:
    branches: [main]
    paths:
      - 'services-spring/payment-service/**'
      - '.github/workflows/payment-service-ci-cd.yml'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - prod

jobs:
  ci-cd:
    uses: ./.github/workflows/reusable-spring-ci-cd.yml
    with:
      service-name: payment-service
      ecr-repository: urr/payment
      service-path: services-spring/payment-service
      environment: ${{ github.event.inputs.environment || 'staging' }}
    secrets: inherit
