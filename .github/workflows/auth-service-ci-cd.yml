name: Auth Service CI/CD

on:
  push:
    branches: [main]
    paths:
      - 'services-spring/auth-service/**'
      - '.github/workflows/auth-service-ci-cd.yml'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - prod

jobs:
  ci-cd:
    uses: ./.github/workflows/reusable-spring-ci-cd.yml
    with:
      service-name: auth-service
      ecr-repository: urr/auth
      service-path: services-spring/auth-service
      environment: ${{ github.event.inputs.environment || 'staging' }}
    secrets: inherit
